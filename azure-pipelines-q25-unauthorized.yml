trigger: none

pool:
  vmImage: ubuntu-latest

steps:

- task: AzureCLI@2
  displayName: Test Restricted Connection
  inputs:
    azureSubscription: 'q25-secure-azure'
    scriptType: bash
    scriptLocation: inlineScript
    inlineScript: |
      az account show
